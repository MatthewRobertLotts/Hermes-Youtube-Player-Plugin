# YouTube Float v3.113

## Overview
Documents the plugin security/privacy model and adds a small CI guard for session safety.

## Changes
- Adds `SECURITY.md`.
- Adds README privacy/security summary.
- Adds release-check guard for obvious cookie/session/token access patterns.
- Keeps all release files on `v3.113`.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- GitHub Actions workflow
