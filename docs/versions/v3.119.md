# YouTube Float v3.119

## Overview
Repairs changelog history and guards it in CI.

## Changes
- Restores v3.108 through v3.118 changelog headings.
- Adds duplicate-version detection to release checks.
- Keeps release packaging and tests unchanged.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- `python3 scripts/build-release-zip.py --check`
- GitHub Actions workflow
