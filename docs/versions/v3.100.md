# YouTube Float v3.100

## Overview
Retry signed History feed loading.

## Changes
- Updates `plugin.js` to `v3.100`.
- Updates the current installer to `install-youtube-float-v3.100.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

