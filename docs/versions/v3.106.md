# YouTube Float v3.106

## Overview
Make History a single-writer flow.

## Changes
- Updates `plugin.js` to `v3.106`.
- Updates the current installer to `install-youtube-float-v3.106.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

