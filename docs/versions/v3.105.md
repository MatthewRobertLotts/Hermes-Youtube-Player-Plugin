# YouTube Float v3.105

## Overview
Prevent stale History overwrites.

## Changes
- Updates `plugin.js` to `v3.105`.
- Updates the current installer to `install-youtube-float-v3.105.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

