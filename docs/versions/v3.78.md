# YouTube Float v3.78

## Overview
Autoload dashboard shelves in background.

## Changes
- Updates `plugin.js` to `v3.78`.
- Updates the current installer to `install-youtube-float-v3.78.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

