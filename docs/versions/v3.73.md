# YouTube Float v3.73

## Overview
Add compact dashboard shelf rows.

## Changes
- Updates `plugin.js` to `v3.73`.
- Updates the current installer to `install-youtube-float-v3.73.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

