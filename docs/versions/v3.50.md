# YouTube Float v3.50

## Overview
Caps how far the docked pane can be stretched.

## Changes
- Adds `maxWidth: '820px'` to the docked pane contribution data.
- Does not change player/video CSS.
- Keeps fixed preset sizing and account persistence.

## Verification
- `node --check plugin.js`
- Runtime render harness confirmed docked pane data includes `maxWidth: '820px'`.
- Static checks confirmed no `dockResponsive`, no `fitScript`, no `object-fit`.
