# YouTube Float v3.30

## Overview
Adds switchable placement without duplicate players.

## Behavior
- `Place` dropdown offers Docked/Floating.
- Switching disposes the current pane contribution and registers the same `player` contribution with new pane data.
- Choice persists in plugin storage.

## Verification
- `node --check plugin.js`
- Runtime import stub confirmed one `panes:player` registration plus route/sidebar/status/palette.
