# YouTube Float v3.66

## Overview
Add the native YouTube dashboard route.

## Changes
- Updates `plugin.js` to `v3.66`.
- Updates the current installer to `install-youtube-float-v3.66.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

