# YouTube Float v3.51

## Overview
Adds the next fullscreen mode iteration after v3.50.

## Changes
- Updates `plugin.js` to `v3.51`.
- Updates the current installer to `install-youtube-float-v3.51.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

