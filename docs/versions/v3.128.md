# YouTube Float v3.128

## Overview
Long-session stability: stop reloading hidden dashboard webviews on churn; bound the idle poll.

## Changes
- Stable cache-busted `src` per persistent hidden webview (no reload on re-render or swap).
- Documented profiling steps in `PERFORMANCE.md`.
- Deterministic tests for `stableSourceFor` and `boundedInterval`.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- zip builder, release notes, publish dry-run, installer syntax + smoke, GitHub Actions
