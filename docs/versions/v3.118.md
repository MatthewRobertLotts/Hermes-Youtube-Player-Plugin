# YouTube Float v3.118

## Overview
Adds reproducible release zip packaging.

## Changes
- Adds `scripts/build-release-zip.py`.
- Adds CI package check for zip contents.
- Updates release docs to use the builder instead of manual zipping.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- `python3 scripts/build-release-zip.py --check`
- GitHub Actions workflow
