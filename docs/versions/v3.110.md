# YouTube Float v3.110

## Overview
Adds the first predictable automated tests without trying to automate Electron or live YouTube.

## Changes
- Adds `src/youtube-core.mjs` for small pure helper logic.
- Adds `tests/youtube-core.test.mjs`.
- Covers video ID extraction, playlist IDs, Shorts detection, timestamps, watch URL building, dashboard shelf normalisation, and queue advancement.
- Runs tests in CI.

## Verification
- `node --test tests`
- `node scripts/check-release.mjs`
- GitHub Actions workflow
