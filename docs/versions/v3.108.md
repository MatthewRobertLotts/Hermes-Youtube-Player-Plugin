# YouTube Float v3.108

## Overview
Locks the current product to one canonical release version across plugin source, docs, installers, and tag naming.

## Changes
- `plugin.js` version constant is `v3.108`.
- Windows installer is `install-youtube-float-v3.108.ps1`.
- macOS/Linux installer is `install-youtube-float-v3.108.sh`.
- README and changelog use `v3.108` consistently.

## Verification
- `node --check plugin.js`
- `sh -n install-youtube-float-v3.108.sh`
- Linux/temp-HOME installer clean install smoke test
- Linux/temp-HOME upgrade smoke test preserving user-state-adjacent files
- Windows PowerShell installer marker/path review
- macOS path smoke through shell installer

