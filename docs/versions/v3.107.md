# YouTube Float v3.107

## Overview
Load History through YouTube browse API.

## Changes
- Updates `plugin.js` to `v3.107`.
- Updates the current installer to `install-youtube-float-v3.107.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

