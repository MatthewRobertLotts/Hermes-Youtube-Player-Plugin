# YouTube Float v3.109

## Overview
Adds automated CI and release consistency checks.

## Changes
- Adds `.github/workflows/check.yml`.
- Adds `manifest.json`.
- Adds `scripts/check-release.mjs`.
- Adds a README GitHub Actions status badge.
- Keeps plugin, README, changelog, installers, and release tag on `v3.109`.

## Verification
- `node scripts/check-release.mjs`
- `sh -n install-youtube-float.sh`
- `sh -n install-youtube-float-v3.109.sh`
- Shell installer temp-HOME smoke test

