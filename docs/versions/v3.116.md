# YouTube Float v3.116

## Overview
Adds diagnostics-first GitHub issue intake.

## Changes
- Adds `.github/ISSUE_TEMPLATE/bug_report.yml`.
- Adds issue-template config with latest-release link.
- Adds CI checks for required bug-report fields.
- Keeps feature-request workflow out until it is needed.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- GitHub Actions workflow
