# YouTube Float v3.127

## Overview
Makes docked/floating and close/reopen lifecycle dependable without redesigning either layout.

## Changes
- Queue + queue keep continuity across placement swaps and reopen.
- Registration disposes the prior pane before registering the next, preventing duplicate players.
- Deterministic lifecycle bookkeeping tests + runtime parity guards.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- `python3 -m py_compile scripts/*.py`
- `python3 scripts/build-release-zip.py --check`
- `python3 scripts/changelog-release-notes.py`
- `python3 scripts/publish-release.py --dry-run`
- installer syntax + smoke
- GitHub Actions workflow
