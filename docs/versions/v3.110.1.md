# YouTube Float v3.110.1

## Overview
Fixes CI test discovery for the new Phase 3 test suite.

## Changes
- `scripts/check-release.mjs` now discovers `tests/*.test.mjs` using Node filesystem APIs instead of relying on shell glob expansion.
- Keeps all release files on `v3.110.1`.

## Verification
- `node scripts/check-release.mjs`
- `node --test tests/*.test.mjs`
- GitHub Actions workflow
