# YouTube Float v3.85

## Overview
Restore signed History filtering.

## Changes
- Updates `plugin.js` to `v3.85`.
- Updates the current installer to `install-youtube-float-v3.85.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

