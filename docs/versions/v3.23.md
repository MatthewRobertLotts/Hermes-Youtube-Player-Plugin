# YouTube Float v3.23

## Overview
Adds a minimal Mini mode and cleans account-feed loading status text.

## Changes
- Mini mode shrinks the pane to video + timeline + volume + Full button.
- Removed dead own-channel resolver code.
- Size persistence is deferred.

## Verification
- `node --check plugin.js`
- Repo/zip contain no built-in default search term and no hardcoded YouTube API-key pattern.
