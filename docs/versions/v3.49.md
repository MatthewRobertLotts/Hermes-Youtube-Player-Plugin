# YouTube Float v3.49

## Overview
Restores safe fixed preset docked sizing after responsive scaling experiments regressed smaller panes.

## Changes
- Removes `dockResponsive` render path.
- Removes cover/object-fit experiment remnants.
- Player box uses the selected preset height again.
- Keeps account label persistence.

## Verification
- `node --check plugin.js`
- Runtime import/render harness confirmed docked pane renders.
- Static checks confirmed no `dockResponsive`, no `fitScript`, no `object-fit`.
