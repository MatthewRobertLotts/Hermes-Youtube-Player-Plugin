# YouTube Float v3.96

## Overview
Add the dashboard stats panel.

## Changes
- Updates `plugin.js` to `v3.96`.
- Updates the current installer to `install-youtube-float-v3.96.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

