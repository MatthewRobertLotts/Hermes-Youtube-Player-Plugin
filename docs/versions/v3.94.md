# YouTube Float v3.94

## Overview
Match top borders to scrollbar colour.

## Changes
- Updates `plugin.js` to `v3.94`.
- Updates the current installer to `install-youtube-float-v3.94.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

