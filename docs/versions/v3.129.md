# YouTube Float v3.129

## Overview
Polished responsive UX and accessibility without redesigning the successful visual design.

## Changes
- Scoped `:focus-visible` outline + disabled cursor for clear keyboard focus (player & dashboard).
- `aria-label="Seek position"` on the timeline slider.
- Narrow-window dashboard reflow (media/stat panes + metric grids stack via media query).
- Kept the keyboard surface minimal: only Escape exits fullscreen, so Hermes chat/input is never
  hijacked.
- New responsive/accessibility manual checklist.

## Verification
- `node --check plugin.js`, `node --test`, `check-release`, zip, notes, publish dry-run, installers, Actions.
