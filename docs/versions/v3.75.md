# YouTube Float v3.75

## Overview
Add live dashboard controls and row stats.

## Changes
- Updates `plugin.js` to `v3.75`.
- Updates the current installer to `install-youtube-float-v3.75.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

