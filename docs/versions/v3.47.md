# YouTube Float v3.47

## Overview
Fixes the v3.46 render-time ReferenceError.

## Root Cause
The player effect referenced `dockResponsive` before the const was initialized.

## Changes
- Moves `cfg`, `mini`, and `dockResponsive` above the effects that read them.
- Keeps docked cover-fill logic.

## Verification
- `node --check plugin.js`
- Runtime import/render harness confirmed docked pane renders without ReferenceError.
