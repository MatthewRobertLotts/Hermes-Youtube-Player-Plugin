# YouTube Float v3.62

## Overview
Package the host fullscreen bridge trial.

## Changes
- Updates `plugin.js` to `v3.62`.
- Updates the current installer to `install-youtube-float-v3.62.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

