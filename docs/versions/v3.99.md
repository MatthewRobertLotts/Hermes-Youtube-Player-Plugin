# YouTube Float v3.99

## Overview
Flatten the top bar and restore signed History path.

## Changes
- Updates `plugin.js` to `v3.99`.
- Updates the current installer to `install-youtube-float-v3.99.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

