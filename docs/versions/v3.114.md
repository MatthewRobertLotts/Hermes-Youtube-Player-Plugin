# YouTube Float v3.114

## Overview
Adds release engineering docs and prepares the first stable GitHub Release.

## Changes
- Adds `RELEASE_CHECKLIST.md`.
- Adds README release/update guidance.
- Keeps plugin, manifest, README, changelog, installers, and tag on `v3.114`.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- GitHub Actions workflow
- GitHub Release asset verification
