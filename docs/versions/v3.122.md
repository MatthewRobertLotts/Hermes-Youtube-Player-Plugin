# YouTube Float v3.122

## Overview
Strengthens tests against the shipped runtime logic without changing media/UI features.

## Changes
- Adds `tests/plugin-runtime-parity.test.mjs`.
- Expands `tests/youtube-core.test.mjs`.
- Adds shared queue/prefs helpers in `src/youtube-core.mjs` for deterministic offline checks.
- Keeps `plugin.js` self-contained because release zips intentionally ship one runtime plugin file.

## Verification
- `node --check plugin.js`
- `node --test tests/*.test.mjs`
- `node scripts/check-release.mjs`
- `python3 scripts/build-release-zip.py --check`
- `python3 scripts/changelog-release-notes.py`
- `python3 scripts/publish-release.py --dry-run`
- GitHub Actions workflow
