# YouTube Float v3.64

## Overview
Keep the plugin-only Big Screen fallback.

## Changes
- Updates `plugin.js` to `v3.64`.
- Updates the current installer to `install-youtube-float-v3.64.ps1`.
- Updates README and changelog metadata for this version.

## Verification
- `node --check plugin.js`

